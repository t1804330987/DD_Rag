spring:
  application:
    name: dd-rag
  profiles:
    active: dev
  autoconfigure:
    exclude:
      - com.alibaba.cloud.ai.autoconfigure.dashscope.DashScopeAgentAutoConfiguration
      - com.alibaba.cloud.ai.autoconfigure.dashscope.DashScopeChatAutoConfiguration
      - com.alibaba.cloud.ai.autoconfigure.dashscope.DashScopeEmbeddingAutoConfiguration
  servlet:
    multipart:
      max-file-size: 256MB
      max-request-size: 256MB

ddrag:
  security:
    # AES-256 master secret for encrypting model connection API keys at rest.
    # Override in production: DD_RAG_API_KEY_ENCRYPTION_SECRET
    api-key-encryption-secret: ${DD_RAG_API_KEY_ENCRYPTION_SECRET:dd-rag-dev-api-key-encryption-secret-2026}
  model-runtime:
    assistant:
      max-active-turns: 50
    invocation:
      global-limit: 50
      per-user-limit: 2
      default-connection-limit: 50
    stream:
      executor-threads: 50
      queue-capacity: 0
    timeout:
      first-token: 60s
      idle: 45s
      business-total: 5m
      transport-hard: 6m
